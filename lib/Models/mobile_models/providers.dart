import 'package:flutter/cupertino.dart';

class EventProvider {
  String? imageUrl;
  String? name;
  String? type;
  String? location;

  EventProvider({this.imageUrl, this.name, this.type, this.location});
}
